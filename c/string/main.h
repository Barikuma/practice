#ifndef MAIN_H
#define MAIN_H

// header files
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

// function prototypes
void _strcpy(char *src, char *des);
int _strlen(char *s);
char * toCap(char *s);
char * toTitle(char *s);

#endif